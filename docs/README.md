# docs

- `assets/`: screenshots of the live site used by the root README.
- `build-log.md`: dated build log from the identity spike to the live domain.
- `mera-spike-report.md`: Mera 0.2.0 API findings, 17 derivation vectors, blockers and how each closed.
- `device-matrix.md`: real-device results (Android Chrome + Google Password Manager, Windows via hybrid QR), the run-1 failure and the addressing rule it produced.
- `device-reports/`: raw JSON captured by the spike pages on the devices.
- `deploy-monad-testnet.md`: the testnet runbook: keys, funding, deploy, seed events, verification, `SetBaseURI`.
- `final-domain-migration.md`: ordered checklist for moving staging to the submission domain (env, republish, smoke, developer guided run, `baseURI`, docs).
- `envio-hosted-handoff.md`: deploying `packages/indexer` to Envio Cloud, the queries that prove it and what the app reads from it next.
- `demo-video-storyboard.md`: the two-minute cut: measured run timings, capture settings (display capture for the passkey sheet), scenes, B-roll, edit notes.
- `remaining-work-plan.md`: what is done, what the website and product still need, Envio's visible feature, hosting and domain implications for passkeys, device matrix, pre-capture gate, build order to the 14 Oct deadline.
