# docs

Current references:

- `assets/`: screenshots of the live site used by the root README.
- `build-log.md`: dated build log from the identity spike to the live domain.
- `deploy-monad-testnet.md`: the testnet runbook: keys, funding, deploy, seed events, verification, `SetBaseURI`.
- `envio-hosted-handoff.md`: deploying `packages/indexer` to Envio Cloud, the queries that prove it and what the app reads from it.
- `device-matrix.md`: real-device passkey results. Verified: Android Chrome with Google Password Manager and a Windows laptop through the hybrid QR sheet. Other rows are optional confirmation.
- `device-reports/`: raw JSON captured by the spike pages on the devices.
- `mera-spike-report.md`: Mera 0.2.0 API findings, 17 derivation vectors and how each early blocker closed.

Planning records, kept for traceability and marked as superseded at the top of each file:

- `remaining-work-plan.md`: the 14 Sep 2026 plan from staging to the submission origin. Sections 2 to 8 shipped.
- `final-domain-migration.md`: the checklist that was followed to move to `turnstile.work` on 15 Sep 2026.
- `landing-redesign-plan.md` and `landing-v2-proposal.md`: the landing and city redesign proposals. Direction A shipped.
- `demo-video-storyboard.md`: the two-minute cut. The video is captured by the author outside this repo.
